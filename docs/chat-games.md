# Chat Games

6 jenis: math, scramble, fast-type, reverse, trivia, hot-letter.
Auto round tiap `auto-interval-minutes` atau manual `/chatgames <type>`.
Reward via console commands `%player%` di `modules/chatgames/config.yml`.
