# Chat Customization

`/chatcolor` — 16 warna + bold/italic (perm `essentials.chatcustomization.decorations`).
`/tags` — pilih tag simbol; tag tampil SEBELUM rank & nama pemain.

Semua tersimpan di `preferences.json`.
